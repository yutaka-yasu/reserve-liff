# LINE Messaging API / LIFF settings
CHANNEL_SECRET=YOUR_CHANNEL_SECRET
CHANNEL_ACCESS_TOKEN=YOUR_CHANNEL_ACCESS_TOKEN
LIFF_ID=YOUR_LIFF_ID

# Optional: Reminder timezone and store
TIMEZONE=Asia/Tokyo

# Storage (choose one, defaults to local JSON file db)
STORAGE=local_json
JSON_DB_PATH=./data/reservations.json

# If using Google Sheets (optional), fill these and set STORAGE=google_sheets
GOOGLE_SHEETS_CREDENTIALS_JSON=./secrets/google-service-account.json
GOOGLE_SHEETS_SPREADSHEET_ID=YOUR_SPREADSHEET_ID